# Deployment topology

> What the post-merge pipeline covers automatically vs what stays manual.
> Read by `strategic-pm` at session start. Used to build the wrap-up
> message after a sprint completes.

## 1. Pipeline-covered actions (automatic on merge)

[List each pipeline step that runs without PO intervention.]

## 2. Manual hors-CI actions (PO clicks merge AND does these)

[List actions that cannot be pipeline-automated and require the PO.]

## 3. Pre-merge manual checks (PO does these BEFORE clicking merge)

[List checks the pipeline cannot perform.]

## 4. Auto-rollback triggers

[List rollback conditions if the pipeline supports auto-rollback.]
